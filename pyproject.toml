[project]
name = "thesis-writing-agent"
version = "0.1.0"
description = "An AI agent for assisting with thesis writing"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
